const { TLArg } = require('./tlarg')
const { TLObject } = require('./tlobject')
const { parseTl, findLayer } = require('./parser')

module.exports = {
    TLArg,
    TLObject,
    parseTl,
    findLayer,
}
