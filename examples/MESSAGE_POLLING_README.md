# Message Polling Script

Скрипт для тестирования polling'а сообщений в GramJS. Отлавливает все входящие сообщения в реальном времени с использованием структурированного MessageListener API.

## 📋 Возможности

- ✅ Отлавливает все входящие сообщения (личные, групповые, каналы)
- ✅ Показывает детальную информацию о каждом сообщении
- ✅ Отображает информацию об отправителе и чате
- ✅ Определяет тип сообщения (текст, медиа, с кнопками и т.д.)
- ✅ Красивое форматирование вывода
- ✅ Сохранение сессии для повторного использования
- ✅ **Структурированный API для обработки сообщений**
- ✅ **Поддержка множественных обработчиков**
- ✅ **Встроенная статистика**

## 🚀 Использование

### Вариант 1: Базовый (JavaScript)

Простой мониторинг всех сообщений с выводом в консоль:

```bash
node examples/messagePolling.js
```

### Вариант 2: Продвинутый (JavaScript)

С дополнительными обработчиками, статистикой и примерами авто-ответов:

```bash
node examples/messagePollingAdvanced.js
```

### Вариант 3: TypeScript версия

```bash
npx ts-node examples/messagePolling.ts
```

## ⚙️ Настройка

Откройте файл `messagePolling.js` или `messagePolling.ts` и заполните:

```javascript
const apiId = 0; // Ваш API ID от my.telegram.org
const apiHash = ""; // Ваш API Hash от my.telegram.org
const stringSession = ""; // Оставьте пустым при первом входе
```

### Где взять API ID и API Hash?

1. Перейдите на https://my.telegram.org
2. Войдите в свой аккаунт
3. Перейдите в "API development tools"
4. Создайте приложение и получите `api_id` и `api_hash`

## 📝 Пример вывода

```
🚀 Starting Message Polling Script...

📡 Connecting to Telegram...
✅ Connected successfully!

👤 Logged in as: John Doe (@johndoe)
User ID: 123456789

🔑 Your session string (save this for next time):
1BVtsOLgBu1JMW...

👂 Listening for messages...
Press Ctrl+C to stop

============================================================
📨 NEW MESSAGE RECEIVED
============================================================
Message ID: 12345
Date: 10/22/2025, 3:45:30 PM
Text: Hello, world!
Sender ID: 987654321
Sender: Jane Smith (@janesmith)
Chat ID: 987654321
Type: Private message
============================================================
```

## 🎯 Что отображается для каждого сообщения

- **Message ID** - уникальный идентификатор сообщения
- **Date** - дата и время получения
- **Text** - текст сообщения
- **Sender ID** - ID отправителя
- **Sender** - имя и username отправителя
- **Chat ID** - ID чата
- **Chat** - название чата/группы
- **Type** - тип сообщения (Private/Group/Channel)
- **Media** - наличие медиа-файлов
- **Reply to** - если это ответ на другое сообщение
- **Forwarded** - если сообщение переслано
- **Buttons** - наличие кнопок

## 💡 Советы

### Сохранение сессии

После первого входа скрипт выведет строку сессии. Сохраните её и вставьте в переменную `stringSession`:

```javascript
const stringSession = "1BVtsOLgBu1JMW..."; // Вставьте сюда
```

Это позволит не вводить код каждый раз при запуске.

### Фильтрация сообщений

Вы можете модифицировать `NewMessage({})` для фильтрации:

```javascript
// Только личные сообщения
new NewMessage({ incoming: true, outgoing: false })

// Только от определенного пользователя
new NewMessage({ fromUsers: [123456789] })

// Только из определенного чата
new NewMessage({ chats: [123456789] })
```

### Включение debug логов

Раскомментируйте в TypeScript версии:

```typescript
Logger.setLevel("debug");
```

## 🛑 Остановка скрипта

Нажмите `Ctrl+C` для корректной остановки и отключения от Telegram.

## ⚠️ Важно

- Не делитесь своим `stringSession` - это полный доступ к вашему аккаунту
- Не коммитьте файлы с заполненными `apiId`, `apiHash` и `stringSession` в Git
- Скрипт будет работать, пока не будет остановлен

## 🔧 Troubleshooting

### "Error: Cannot find module '../dist'"

Сначала соберите проект:

```bash
npm run build
```

### "API ID invalid"

Проверьте, что вы правильно скопировали API ID и API Hash с https://my.telegram.org

### "Phone number invalid"

Убедитесь, что вводите номер телефона с кодом страны, например: `+79991234567`

## 🎨 Примеры использования API

### Базовый пример

```javascript
const messageListener = new MessageListener(client);
messageListener.init();

// Простой обработчик
messageListener.on("message", (data) => {
    console.log(`Message from ${data.sender?.fullName}: ${data.text}`);
});
```

### Фильтрация сообщений

```javascript
// Только личные сообщения
messageListener.on("message", (data) => {
    if (data.type === "private") {
        console.log(`Private: ${data.text}`);
    }
});

// Только групповые
messageListener.on("message", (data) => {
    if (data.type === "group") {
        console.log(`Group: ${data.text}`);
    }
});
```

### Авто-ответ

```javascript
messageListener.on("message", async (data) => {
    if (data.text && data.text.toLowerCase() === "ping") {
        await client.sendMessage(data.chatId, {
            message: "Pong! 🏓"
        });
    }
});
```

### Детекция ключевых слов

```javascript
const keywords = ["важно", "срочно", "alarm"];

messageListener.on("message", (data) => {
    if (data.text) {
        const found = keywords.find(kw => 
            data.text.toLowerCase().includes(kw)
        );
        if (found) {
            console.log(`🚨 Keyword "${found}" detected!`);
        }
    }
});
```

### Статистика

```javascript
// Вывод статистики каждую минуту
setInterval(() => {
    const stats = messageListener.getStats();
    console.log(`Messages: ${stats.messageCount}, Rate: ${stats.messagesPerMinute}/min`);
}, 60000);
```

### Множественные обработчики

```javascript
messageListener
    .on("message", logToConsole)
    .on("message", saveToDatabase)
    .on("message", detectSpam)
    .on("message", sendWebhook);
```

## 📚 Документация

Полная документация по MessageListener API доступна в файле:
- [MESSAGE_LISTENER_API.md](MESSAGE_LISTENER_API.md)

В ней вы найдете:
- Подробное описание всех методов
- Структуру данных messageData
- 10+ примеров использования
- Лучшие практики
- Интеграции (webhook, БД, файлы)

